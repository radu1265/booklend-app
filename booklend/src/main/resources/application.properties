spring.application.name=booklend
# Server Configuration
server.port=8080
file.upload-dir=uploads

# Database Configuration (PostgreSQL)
# Update these with your actual local Postgres credentials
spring.datasource.url=jdbc:postgresql://localhost:5432/booklend_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- OR Use H2 for In-Memory Testing (Uncomment below, Comment above) ---
# spring.datasource.url=jdbc:h2:mem:booklend
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# Security Logging
logging.level.org.springframework.security=INFO

jwt.secret=change_this_to_a_long_random_secret_at_least_32_chars
jwt.expiration=86400000